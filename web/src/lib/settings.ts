// Display settings are now managed server-side per display.
// This module is kept for backward compatibility but no longer stores anything in localStorage.
// All types have moved to @/lib/types.ts

export type { SleepMode, DisplayConfig as DisplaySettings } from "./types"
